/**
 * useCatalogCategories — TanStack Query backed
 *
 * Re-exports from the canonical query hook.
 * Kept for backward compatibility with existing import paths.
 */

export type { CatalogCategory, CatalogCategoryGroup } from '@/lib/hooks/queries/catalog';
export { useCatalogCategories } from '@/lib/hooks/queries/catalog';
