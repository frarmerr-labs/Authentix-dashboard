/**
 * useCatalogSubcategories — TanStack Query backed
 *
 * Re-exports from the canonical query hook.
 * Kept for backward compatibility with existing import paths.
 */

export type { CatalogSubcategory } from '@/lib/hooks/queries/catalog';
export { useCatalogSubcategories } from '@/lib/hooks/queries/catalog';
