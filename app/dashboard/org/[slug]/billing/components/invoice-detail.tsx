/**
 * InvoiceDetail — legacy component, no longer used.
 * Invoice detail is now rendered inline in billing/invoices/[id]/page.tsx.
 * @deprecated
 */

export function InvoiceDetail(_props: {
  invoiceId: string;
  companyName: string;
  companyEmail: string;
}) {
  return null;
}
